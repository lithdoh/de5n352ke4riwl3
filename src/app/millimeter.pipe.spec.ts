import { MillimeterPipe } from './millimeter.pipe';

describe('MillimeterPipe', () => {
  it('create an instance', () => {
    const pipe = new MillimeterPipe();
    expect(pipe).toBeTruthy();
  });
});
