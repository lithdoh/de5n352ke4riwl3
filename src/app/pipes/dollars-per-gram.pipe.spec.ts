import { DollarsPerGramPipe } from './dollars-per-gram.pipe';

describe('DollarsPerGramPipe', () => {
  it('create an instance', () => {
    const pipe = new DollarsPerGramPipe();
    expect(pipe).toBeTruthy();
  });
});
